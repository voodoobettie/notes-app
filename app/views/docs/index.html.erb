<%= @docs.each do |doc| %>
  <h2><%= link_to doc.title, doc %></h2>
  <p><%= time_ago_in_words(doc.created_at) %></p>
  <p><%= truncate(doc.content, length:50) %></p>
<% end %>
  <h3><%= link_to "Create new doc", new_doc_path %></h3>
